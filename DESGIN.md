# DESIGN.md

## 0) 문제 정의

과제의 핵심은 복잡한 도면 데이터(`도면 → 공종 → Region → Revision`)에서 사용자가 빠르게 원하는 정보에 도달하도록 만드는 것입니다.
필수 기능은 다음 3가지로 해석했습니다.

1. 도면 탐색
2. 도면 표시
3. 컨텍스트 인식

---

## 1) 데이터 분석

### 1-1. metadata.json 해석 방식

원본 `metadata.json`을 다음 두 관점으로 분리해서 해석했습니다.

1. 탐색 관점: `도면 → 공종 → Region(옵션) → Revision`
2. 렌더링 관점: 이미지/폴리곤/transform이 있는 "레이어" 단위

그래서 내부 모델을 2개로 분리했습니다.

- `NavigationIndex`: 셀렉터 UI용
- `RenderLayer[]`: 뷰어 렌더링용

### 1-2. 특수 케이스 해석

다음 케이스를 별도 분기로 처리했습니다.

1. 101동 구조: `regions.A/B` 아래에 revision 존재
2. 주민공동시설 건축: discipline 레벨이 아니라 revision 레벨에 `imageTransform/polygon`
3. 주차장 구조: polygon 없음
4. revision에 transform이 없는 경우: `rev.imageTransform ?? disc.imageTransform` fallback

### 1-3. 더 나은 데이터 표현 방법

개선한다면 서버/전처리 단계에서 아래를 제공하는 게 좋을 것으로 보입니다.

1. 완전 평탄화된 `render_layers`
2. 모든 revision에 명시적 transform
3. 동일 도면군의 기준 좌표계(reference) 명시
4. 변경영역(changes)의 좌표 정보(현재는 텍스트 중심)

---

## 2) 접근 방식

### 2-1. 해결 순서

1. metadata 분석 + 타입/검증(Zod)
2. NavigationIndex / RenderLayer 정규화
3. 모바일 뷰어(표시, zoom/pan, polygon)
4. 모바일 선택 바텀시트(초기 진입/재진입)
5. 데스크탑 확장(좌:제어, 중:뷰어, 우:인스펙터)
6. 공종 오버레이 + opacity
7. 정합 이슈 보정(공종별 미세 보정)

### 2-2. 왜 이 순서였는가

UI를 먼저 만들면 데이터 분기 때문에 코드가 빠르게 복잡해져서,
먼저 정규화 계층을 만든 뒤 UI를 얹는 방식이 리스크가 작을것으로 판단했습니다.

---

## 3) UI 설계 결정

### 3-1. 선택한 레이아웃과 이유

모바일:

- 상단: 현재 컨텍스트(도면/공종/리비전)
- 중앙: 뷰어
- 하단: 변경 항목
- 도면 선택: 바텀시트

이유:

- 작은 화면에서 탐색 진입점을 고정(바텀시트)
- 컨텍스트/결과(뷰어)/작업(변경 항목) 구분이 명확

데스크탑:

- 좌측 패널: 탐색/오버레이 제어
- 중앙: 캔버스
- 우측: 인스펙터

이유:

- 분석 작업에서는 제어와 결과를 동시에 보는 3분할이 효율적

### 3-2. 고려한 대안과 장단점

대안 A: 상단 탭 + 모달 탐색

- 장점: 화면이 깔끔
- 단점: 탐색 깊이가 깊을수록 왕복 비용 증가

대안 B: 트리 사이드바 고정(모바일 포함)

- 장점: 구조 가시성 높음
- 단점: 모바일에서 뷰어 면적 크게 손실

최종 선택:

- 모바일은 바텀시트, 데스크탑은 고정 패널로 분리

---

## 4) 기술 선택

### 4-1. 상태 관리

- `@reduxjs/toolkit`, `react-redux` 채택
- 이유: 선택 상태/오버레이 상태를 일관되게 확장하기 쉬움
- 빠른 프로토타입 진행 과정에서 일부 로컬 상태가 남아 있어 추후 통합필요

### 4-2. 렌더링

- `konva`, `react-konva` 채택
- 이유: pan/zoom/레이어/transform 처리에 적합

### 4-3. 데이터 안정성

- `zod` 채택
- 이유: 실데이터의 nullable/옵셔널 필드 편차를 런타임에서 조기 검출

### 4-4. 스타일

- Tailwind CSS 채택
- 이유: 빠른 화면 반복 수정과 반응형 구성에 유리

---

## 5) 어려웠던 점 및 개선 방안

### 5-1. 어려웠던 점

1. 도면별 이미지 해상도/기준 좌표가 서로 다름
2. revision/discipline마다 transform 제공 형태가 다름
3. 브라우저 네이티브 select의 모바일 팝업 위치 이슈

### 5-2. 실제 대응

1. 정규화 계층 도입(NavigationIndex/RenderLayer)
2. transform fallback(`rev ?? disc`)
3. 모바일 select를 커스텀 드롭다운(포털+fixed)로 교체
4. 공종별 미세 정합 오차는 `dx/dy/ds` 보정맵으로 제어

### 5-3. 시간이 더 주어지면

- 리비전 비교 전용 모드(split/reveal) 고도화
- 변경 항목과 도면 좌표 자동 연결

---

## 6) 미해결 문제 / 시도한 방법론 / 대안

### 6-1. 미해결 문제 정의

- 일부 조합에서 0.x% 단위의 미세 정합 오차가 여전히 발생 가능

### 6-2. 시도한 방법

1. transform 상대변환 일반화 시도
2. reference 이미지 기준 오버레이 파이프라인 고정
3. revision transform 누락 fallback
4. 케이스 기반 미세 보정 적용

### 6-3. 시도 가능한 대안

1. 이미지 특징점 매칭(자동 정합)
2. 서버 전처리로 모든 레이어를 동일 reference로 변환 후 전달
3. discipline별 보정 프로파일 파일화(JSON)
